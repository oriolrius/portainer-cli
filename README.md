# Building environment

```bash
pipreqs . --force

pip install --editable .

portainer --help
```

# Usage from docker
  
```bash
docker run --rm --network host registry.nexiona.io/nexiona/portainer-cli --help
```

